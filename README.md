# Handwritten-Digit-Recognition-System
 End-to-end ML web app using a TensorFlow/Keras CNN (≥99% accuracy) trained on 60k MNIST samples. Draw a digit on canvas or upload an image to get instant predictions. Flask REST API backend + responsive dark-themed HTML/CSS/JS frontend. No frameworks required.
